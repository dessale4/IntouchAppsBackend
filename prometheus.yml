global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "intouchapps"
    metrics_path: "/intouchApi/v1/actuator/prometheus"
    basic_auth:
      username: "actuator"
      password: "change-me"
    static_configs:
      - targets: ["host.docker.internal:8088"]
